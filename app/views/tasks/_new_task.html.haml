%li{id: "new_task_#{note.id}"}
  - if add
    = link_to "+", new_note_task_path(note), class: "task_new", remote: true
  - else
    = form_for([note, note.tasks.build], remote: true) do |f|
      .field
        = f.text_field(:description, class: 'submittable')
